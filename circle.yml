machine:
  services:
    - docker
  environment:
    # DEBUG_LINTING: true
checkout:
  pre:
    - git config --global user.email "circleci@lintci.com"
    - git config --global user.name "LintCI's CircleCI Bot"
dependencies:
  cache_directories:
    - "~/.docker"
  post:
    - bundle exec rake docker:load docker:build docker:dump docker:tag
deployment:
  master:
    branch: master
    commands:
      - bundle exec rake credentials release docker:push


